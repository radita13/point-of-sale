{
  "name": "point-of-sale",
  "private": true,
  "version": "1.0.0",
  "description": "Point of Sale (POS)",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "pnpm --filter @point-of-sale/backend prisma:generate",
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck"
  }
}
